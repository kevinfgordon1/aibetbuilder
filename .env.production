# Vite bakes VITE_* into the production client (vite build on Vercel).
# Host env wins over this file. Unset is already on; =0 opts New Odds Board
# out of Polymarket and Kalshi. No API keys.
VITE_FIRST_PARTY_PM_LIVE=1
