# Vite bakes VITE_* into the production client (vite build on Vercel).
# Host env wins over this file, so VITE_FIRST_PARTY_PM_LIVE=0 still opts out.
# Betstamp SSE stays on either way. No API keys.
VITE_FIRST_PARTY_PM_LIVE=1
